<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GameDevSimulator.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GameDevSimulator.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource LayerFillColorDefaultBrush}">

    <Grid>
        <Grid.RowDefinitions>

            <RowDefinition Height="50"/>
            <RowDefinition Height="250"/>

        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="50"/>

        </Grid.ColumnDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">
            <TextBlock FontSize="18" Text="Auto-Save"/>
            <TextBlock FontSize="12" Text="Automatically saves game progress"/>
            <ToggleButton/>
        </Grid>

        <Grid Grid.Row="2" Grid.Column="1">
            <TextBlock FontSize="18" Text="Reset Save Data"/>
            <TextBlock FontSize="12" Text="Resets all progress saved"/>
            <ToggleButton/>
        </Grid>
        
    </Grid>
</Page>
